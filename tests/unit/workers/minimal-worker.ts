// minimal-worker.ts
// (this file is compiled & loaded by Miniflare as your Worker, but does absolutely nothing)

export default {
  // You don’t really need to handle fetch here
  // Tests import `env` directly from "cloudflare:test".
};
